server:
  port: 8090

eureka:
  server:
    username: eureka
    password: 114514
  instance:
    instance-id: ${spring.application.name}:${server.port}
    prefer-ip-address: true
  client:
    serviceUrl:
      defaultZone: http://${eureka.server.username}:${eureka.server.password}@localhost:8100/eureka/

spring:
  application:
    name: client-gateway
  redis:
    database: 0
    host: localhost
    port: 6379
    password: 114514
    timeout: 1500
    jedis:
      pool:
        max-idle: 10
        max-wait: -1ms
        min-idle: 2
  cloud:
    gateway:
      discovery:
        locator:
          enabled: false
          lower-case-service-id: true
      globalcors:
        cors-configurations:
          '[/**]':
            allowedOrigins:
              - http://10.135.37.106
            allowedHeaders: "*"
            allowCredentials: true
            allowedMethods:
              - GET
              - POST
              - DELETE
              - PUT
      routes:
        - id: route-search
          uri: lb://client-search
          order: 0
          predicates:
            - Path=/search/**
          filters:
            - StripPrefix=1
